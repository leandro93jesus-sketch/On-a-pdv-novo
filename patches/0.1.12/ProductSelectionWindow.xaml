<Window x:Class="OncaPDV.Desktop.ProductSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecionar produto" Width="980" Height="620" WindowStartupLocation="CenterOwner" Background="#F7F9F8">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Text="PRODUTOS ENCONTRADOS" FontSize="23" FontWeight="Bold" Foreground="#0B6B3A"/>
        <TextBlock Grid.Row="1" x:Name="InfoText" Margin="0,6,0,12" Foreground="#5B6862"/>
        <DataGrid Grid.Row="2" x:Name="ProductsGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="Select_Click" FontSize="15" RowHeight="38">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Binding="{Binding InternalCode}" Width="135"/>
                <DataGridTextColumn Header="PRODUTO" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="CÓD. BARRAS" Binding="{Binding Barcode}" Width="155"/>
                <DataGridTextColumn Header="PREÇO" Binding="{Binding SalePrice,StringFormat=C}" Width="110"/>
                <DataGridTextColumn Header="ESTOQUE" Binding="{Binding Stock,StringFormat=N2}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="CANCELAR" Padding="18,10" Margin="4" IsCancel="True"/>
            <Button x:Name="ActionButton" Content="ADICIONAR SELECIONADO" Padding="18,10" Margin="4" Background="#0B6B3A" Foreground="White" Click="Select_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>