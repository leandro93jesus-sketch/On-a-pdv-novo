import SalesHistoryModal from './SalesHistoryModal';

/** Aba dedicada: HISTÓRICO DE VENDAS (todas as vendas, paginadas). */
export default function HistoricoVendasPage() {
  return <SalesHistoryModal embedded />;
}
